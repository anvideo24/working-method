# 공용 제품 규칙

> 어디에나 해당하는 규칙만 적는다. 특정 제품의 화면·경로·연동은 그 제품 `RULES.md`에 적는다.
> 겹치면 그 제품 `RULES.md`가 그 제품의 정본이다.

## 1. 돈

- 모든 금액은 통화와 발생일·예정일·확정 상태를 함께 가진다.
- 매출과 비용은 거래처 또는 프로젝트와 연결하지 못하면 `미분류`로 명확히 표시한다.
- 증빙 파일은 원본을 보존하고, 누가 언제 올렸는지 기록한다.
- 견적서, 계약서, 청구서, 발행된 세금계산서는 덮어쓰지 않는다. 수정은 새 버전 또는 수정 발행으로 남긴다.
- 회계·세무 시스템을 임의로 대체하거나 법적 판단을 자동화하지 않는다.

## 2. 승인

- 권한은 대표/관리자, 팀원, 회계 담당자처럼 업무 역할로 부여한다.
- 승인·반려·취소는 실행자, 시각, 사유를 남긴다.
- 승인된 금액이나 정산 상태의 직접 수정은 금지한다. 정정 기록 또는 취소 후 재등록으로 남긴다.
- 삭제는 복구 가능하게 처리하며, 관리자만 수행할 수 있다.
- 이메일 발송, 전자서명 요청, 세금계산서 발행 같은 외부로 나가는 일은 대표 승인 뒤에만 실행한다. 외부 결과를 받지 못하면 완료로 표시하지 않는다.
- 되돌릴 수 없는 일(삭제·비용·외부 발송·권한 변경·운영 배포)은 무엇을 하는지 적은 승인을 받는다. 「1」「진행」 같은 짧은 답은 이런 일의 승인이 아니다.

## 3. AI 권한

- AI 에이전트는 사람 계정과 구분된 시스템 계정으로 등록한다.
- 각 에이전트에는 목적, 허용 업무, 접근 가능한 데이터 범위, 일하는 방식·답변 방식·절차·지침, 구독 모델을 명시한다. 구독 모델은 사람이 쓰는 것과 같은 구독 자리이지, 쓴 만큼 돈이 나가는 API 키 호출이 아니다.
- 기록 저장, 외부 발송, 금액·계약 확정, 권한 변경 능력은 기본으로 꺼 둔다. 대표가 에이전트별로 켠 능력만 실행할 수 있다. 지침 문구만으로 권한은 생기지 않는다.
- 에이전트의 요청·입력 자료·결과·사람 승인 여부·사용한 능력을 작업 이력으로 남긴다.
- 대표가 건별로 승인하거나 그 에이전트에 능력이 켜져 있지 않으면 자동 결제·자동 이체·자동 외부 발행을 실행하지 않는다.
- 근거가 없는 AI 요약은 공식 결정 후보로 올리지 않는다.
- **AI는 제안까지, 확정은 사람 손이다.** 대화에서 자동으로 알아챈 약속·일정·값은 후보로 올리고, 사람이 확정한다. 사람이 손으로 정한 값은 자동 인식이 덮어쓰지 못한다.

## 4. 이력

- 업무, 프로젝트, 매출, 비용, 승인, 권한의 생성·변경·삭제에는 변경 이력을 남긴다.
- 원본은 고치지 않는다. 이력은 뒤에 덧붙인다.
- 외부에서 들어온 자료(메일·통화·회의)는 원문 식별자·발생 시각·연결 이유를 보존한다. 요약만을 정본으로 삼지 않는다.

## 5. 검증

- 새 기능은 정상 흐름과 권한 없는 사용자의 차단 흐름을 각각 확인한다.
- 배포 전에는 데이터 저장, 화면 표시, 새로고침 후 상태 유지, 변경 이력을 한 흐름으로 확인한다.
- 실제 운영에 연결되지 않은 로컬 확인은 운영 반영으로 표현하지 않는다.

## 6. 비밀

- 실데이터, 비밀번호, API 키, 개인 식별 정보, 실제 서버 주소, 고객사명, 이메일은 코드·문서·테스트 고정값·채팅·로그·커밋·PR에 넣지 않는다.
- 민감 정보는 역할별로 최소한만 보여 준다.

## 7. 문서의 단일 원본

- 같은 규칙이나 절차를 여러 문서·화면에 복사하지 않는다. 화면은 정본을 읽어 표시한다.
- 구현·운영 방식이 바뀌면 같은 변경에서 해당 매뉴얼과 변경 기록을 갱신한다. 코드·규칙·안내서는 한 세트다.
- 제품의 지금 상태를 작업 습관 문장으로 바꾸거나, 작업 습관을 제품 규칙인 것처럼 올리지 않는다.

## 8. 한 도구에서만 되는 것은 만들지 않는다

**만들기 전에 묻는다 — 「이걸 다른 도구도 읽고 고칠 수 있나?」** 답이 아니오면 그 방식은 버린다.

- **규칙·지식·지도는 텍스트 파일로 두고 git 에 넣는다.** 어느 도구든 읽고 고치고, 누가 언제 바꿨는지가 남는다.
- **판정기는 도구가 아니라 명령으로 만든다.** 한 도구의 확장 기능 안에 넣으면 나머지는 못 쓴다. 명령 하나면 누구나 부른다.
- **한 도구만 자동으로 부르는 것은 괜찮다** — 그 도구가 편의를 더 갖는 것뿐이고, 나머지는 같은 명령을 손으로 부르면 된다. 막히는 건 **부르는 방법 자체가 그 도구에만 있는 경우**다.
- **파일 이름이 도구마다 다르면, 원본 하나를 두고 나머지는 그것을 가리키는 한 줄만 둔다.** 내용을 복사하면 원본이 둘이 되고 반드시 갈라진다(7절).
- 화면이 필요하면 **서버 없이도 보이는 것**을 먼저 고른다. 서버는 죽고, 죽으면 정작 필요할 때 못 본다.

> 왜 규칙이 됐나 — 2026-09-07, 시스템 전체 지도를 「한 도구에서만 열리는 페이지」로 만들자는 제안이 나왔다.
> 읽기도 고치기도 그 도구만 할 수 있었다. 그날 하루 종일 세운 원칙과 정면으로 어긋나는 제안이었고,
> 그래서 규칙으로 박았다. **편해 보이는 길이 대개 한 도구에 갇힌 길이다.**
