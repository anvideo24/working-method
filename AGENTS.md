# 일을 시작하고 끝내는 법

> 도구(Cursor·Claude Code 등)가 저장소를 열 때 처음 읽는 파일이다. **가리키는 게 일이고, 규칙을 여기에 옮겨 적지 않는다.**
> 일하는 방식 자체는 `HOW.md`, 지켜야 할 것은 `RULES.md`, 겪은 것은 `LESSONS.md`다.

## 0. 이 저장소

일하는 방식의 정본이다. 어떤 도구든, 어떤 제품이든 여기서 읽는다.

| 파일 | 무엇 |
|---|---|
| `RULES.md` | 어디에나 해당하는 제품 규칙 |
| `HOW.md` | 일하는 방식 |
| `LESSONS.md` | 배운 것 |
| `AGENTS.md` | 이 파일. 읽는 순서와 되돌려 적는 자리 |

## 1. 시작할 때 읽는 순서

1. 이 저장소를 최신으로 받는다.
2. `HOW.md` → `LESSONS.md` → `RULES.md`
3. 그다음 **그 제품 저장소의 `RULES.md`**, 이어서 그 제품의 운영 설명.
4. 새 작업을 잡거나 작업 종류가 바뀌면 2·3을 다시 읽는다. 기억으로 대신하지 않는다.

로컬에 클론이 없으면 GitHub `anvideo24/working-method`의 `main`을 읽는다.

클론 위치는 제품 저장소와 **같은 부모 폴더**다.

- Windows: `C:\dev\working-method`
- Mac: 제품 저장소와 같은 부모 폴더의 `working-method`

## 2. 끝낼 때 되돌려 적는 자리

오케이가 나면 `HOW.md` 4절대로 처음 안과 최종안을 비교하고, 달라진 것을 아래로 나눈다.

| 달라진 것 | 적는 자리 |
|---|---|
| 어디에나 해당하는 제품 규칙 | 이 저장소 `RULES.md` |
| 일하는 방식 | 이 저장소 `HOW.md` |
| 같은 실수를 두 번 겪지 않게 할 것 | 이 저장소 `LESSONS.md` |
| 그 제품만의 지금 상태 | 그 제품 저장소 `RULES.md` |
| 그 제품의 운영 설명·변경 기록 | 그 제품 저장소 (코드와 **같은 커밋**에서) |

공용 원문을 제품 저장소에 붙여넣고 고치지 않는다. 원본이 갈라진다.

## 3. 제품 저장소의 `AGENTS.md`

각 제품에도 `AGENTS.md`를 둔다. 그 파일이 담을 것은 **이 파일을 가리키는 한 줄과, 그 제품만의 기동·금지**뿐이다.

- 담는다: 클론 경로, 서버 켜는 명령, 포트, 스키마 변경 절차, 그 제품에서만 금지되는 것
- 담지 않는다: 위 1·2절, `HOW.md`·`RULES.md`·`LESSONS.md`에 이미 있는 것

## 4. 도구 지침 칸에 붙이는 문구

ChatGPT처럼 저장소 파일을 자동으로 안 읽는 도구는, 아래를 프로젝트 지침 칸에 붙인다.
**이 블록이 부트 문구의 유일한 원본이다.** 다른 곳에 옮겨 적지 않는다.

원문을 읽을 수 있으면 읽는 쪽이 낫다. 이 저장소는 공개라 로그인 없이 열린다.

```
일하는 방식의 정본은 GitHub anvideo24/working-method 다. 공개 저장소라 바로 읽을 수 있다.
https://github.com/anvideo24/working-method

시작할 때 AGENTS.md → HOW.md → LESSONS.md → RULES.md 순으로 읽고, 그다음 지금 작업하는
제품 저장소의 RULES.md 를 읽는다. 새 작업을 잡거나 작업 종류가 바뀌면 다시 읽는다.
원문을 못 읽는 상황이면 아래만이라도 지킨다.

[정본]
- 공용 원문을 제품 저장소에 붙여넣고 고치지 않는다. 그 제품만의 차이는 그 제품 RULES.md 에 적는다.
- 오케이가 나면 처음 안과 최종안을 비교한다. 어디에나 해당하면 working-method 를, 그 제품만이면
  그 제품 RULES 를 고친다. 일하는 방식은 HOW.md, 겪은 실수는 LESSONS.md 다.
- 코드·규칙·안내서는 한 세트로 같은 커밋에서 고친다. 하나만 고치면 나머지가 거짓말을 한다.

[공개]
- 작업하는 저장소가 공개일 수 있다. 착수 전에 공개인지 확인한다.
- 공개면 코드·문서·커밋 메시지·PR 본문을 누구나 읽고, 지워도 복제·색인이 남는다.
- 실데이터·비밀번호·API 키·연결 문자열·실제 서버 주소·고객사명·이메일은 어느 쪽이든 넣지 않는다.
  「나중에 정리」는 없다. 넣는 순간 영구 노출이다.

[한 작업 공간을 여럿이 쓴다]
- 같은 폴더에서 다른 도구가 동시에 일한다. 내 것만 있다고 가정하지 않는다.
- 커밋할 것은 이름으로 찍어 담는다. 「전부 담기」는 남의 미완성 작업을 쓸어담는다.
- 한 파일에 내 것과 남의 것이 섞였으면 조각 단위로 거르고, 거른 뒤 남의 표식이 없는지 세어 본다.
- 만든 것은 그날 커밋한다. 안 커밋한 것은 남의 동기화 한 번에 치워진다.
- 되돌릴 수 없는 명령(마이그레이션·배포·삭제) 직전에는 설정을 그 자리에서 다시 읽는다.
  아까 읽은 값으로 판단하지 않는다.
- 값이 이상하면(비었다·0이다) 남이 갈아 끼우는 중일 수 있다. 간격을 두고 두 번 재고 결론 낸다.

[일]
- 승인된 계획 없이 구현하지 않는다. 큰 일은 계획과 완료 기준을 먼저 내고, 사람이 고친 뒤에 코드가 나온다.
- 요청받은 크기로 한다. 조용히 넓히거나 좁히지 않는다.
- 새 작업은 최신 main 에서 새 가지를 딴다. 이미 합쳐진 옛 가지에 이어 붙이지 않는다.
- 완료는 선언이 아니라 증거다. 「내 PC 에서 됐다」는 판정이 아니다.
- 같은 대처로 두 번 실패하면 방식이 틀린 것이다. 세 번째를 하지 않고 원인 종류를 다시 가른다.
- 사실·추론·미확인을 나눠 말한다. 기억이나 오래된 문서로 현재 상태를 단정하지 않는다.

[관문]
- main 병합은 자동 검사 통과가 조건이다. 빨간 검사를 우회하지 않는다.
- 마이그레이션은 확인된 DB 에만 돈다. 관문이 막으면 뚫지 말고 사람에게 알린다.
- 되돌릴 수 없는 일은 무엇을 하는지 적은 승인을 받는다. 「응」「1」은 그런 일의 승인이 아니다.
```

git 을 직접 못 고치는 도구는 고칠 문장만 남기고, 사람이나 다른 도구가 푸시한다.
