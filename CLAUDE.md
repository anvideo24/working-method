@AGENTS.md

<!--
이 파일은 다리다. 규칙의 원본은 AGENTS.md 한 곳뿐이고, 여기에 옮겨 적지 않는다.

왜 필요한가 — 클로드는 AGENTS.md 를 자동으로 읽지 않고 CLAUDE.md 만 읽는다. 그래서 위 한 줄로
원본을 불러온다(Claude Code 공식 문서가 권고하는 방식). 코덱스·커서·코파일럿 등은 AGENTS.md 를
직접 읽으므로 이 파일이 필요 없다. 윈도우는 심볼릭 링크에 관리자 권한이 필요해 import 를 쓴다.

확인 방법 — 세션에서 /context 를 열어 Memory files 목록에 CLAUDE.md 가 있는지 본다.
-->
